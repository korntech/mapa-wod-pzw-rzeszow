# Pliki do katalogu `.github/workflows/`

Katalog `.github/` jest chroniony przed zapisem przez narzędzia zewnętrzne. Przenieś pliki:

```bash
mv -f tools/github-workflows/*.yml .github/workflows/
git rm -r -q tools/github-workflows
```
