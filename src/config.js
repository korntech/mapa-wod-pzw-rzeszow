/* Jedno miejsce odczytu konfiguracji aplikacji (config.json w katalogu głównym). */
import config from '../config.json';

export const SITE = config.site;
export const SUPABASE = config.supabase;
export const MAP = config.map;
export const BASEMAPS = config.basemaps;
export const LINKS = config.links;
export const SNAPSHOT = config.snapshot;
